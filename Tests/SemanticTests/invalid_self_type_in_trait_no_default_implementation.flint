// RUN: %flintc %s --verify

struct trait Trait {
  mutating func noDefaultImplementation(thisParam: inout Self)
  // TODO: write test case for parameter conformance in implementation, specifically for paramters of type Self
}

// An implementation of struct trait Trait not implementing `noDefaultImplementation`
struct Implementation: Trait {
  // expects-error {{}}
}

contract A { }

A :: a <- (any) {
  public init() {}
}
